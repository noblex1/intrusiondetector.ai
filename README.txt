
AI-Powered Intrusion Detection System

Steps to run:
1. Train the model:
   $ python train_model.py

2. Start the app:
   $ streamlit run app.py

3. Upload a CSV file with network traffic features (numerical).

Dataset: Use NSL-KDD or CIC-IDS2017. A small sample file (nsl_kdd_sample.csv) is expected.
